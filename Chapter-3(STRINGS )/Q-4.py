# 4. Replace the double space from problem 3 with single spaces.
name = "Harry is a    good       boy and "

print(name.replace("   ", ""))
# string are immutable which means that you connect change then by running functions on time